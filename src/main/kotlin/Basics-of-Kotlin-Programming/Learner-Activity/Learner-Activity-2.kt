package `Learner-Activity`

fun main() {
    // Differences between Float and Double

    // float
    val floatNumber1 = 0.5857588f // if there is only 1 number on left of decimal (i.e 0 to 9) then it accepts 6-7 digits after decimal
    val floatNumber2 = 5.5879645f
    val floatNumber3 = 56.548977f // if there is only 2 number on left of decimal (i.e 10 to 99) then it accepts 5-6 digits after decimal
    val floatNumber4 = 697.54877f // if there is only 3 number on left of decimal (i.e 100 to 999) then it accepts 4-5 digits after decimal
    val floatNumber5 = 5468.5864f // if there is only 4 number on left of decimal (i.e 1000 to 9999) then it accepts 3-4 digits after decimal
    val floatNumber6 = 86574.555f // if there is only 5 number on left of decimal (i.e 10000 to 9999) then it accepts 2-3 digits after decimal
    val floatNumber7 = 574897.56f // if there is only 6 number on left of decimal (i.e 100000 to 99999) then it accepts 1-2 digits after decimal
    val floatNumber8 = 5788687.5f // if there is only 7 number on left of decimal (i.e 1000000 to 999999) then it accepts 0-1 digits after decimal

    // Double
    val doubleNumber1 = 0.5879624875364796

    // ±3.40282347E+38F i.e. 6-7 significant digits - Float - 4bytes

    // ±1.79769313486231570E+308 i.e. 15-16 significant digits - Double - 8 Bytes





}